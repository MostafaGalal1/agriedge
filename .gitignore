# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# macOS
.DS_Store

# The Edge-IIoTset dataset is not redistributed here. Point
# AGRIEDGE_DATASET_ROOT at your own copy; see the README.
Edge-IIoTset dataset/
*.pcap

# The built benchmark is 37 MB and fully regenerable:
#     python experiments/03_build_agribench.py
# It is archived at doi:10.5281/zenodo.21941319 rather than carried in git.
results/*.parquet

# arXiv submission package (regenerate with: cd paper && make arxiv)
paper/arxiv.tar.gz
paper/arxiv-build/

# LaTeX build intermediates. The PDFs themselves are tracked.
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.spl
paper/*.toc
