%% Provenance, Not Behaviour --- Elsevier (elsarticle) submission source.
%%
%% Compile:  tectonic -X compile manuscript.tex      (or: make)
%%
%% This file is a thin class wrapper. The prose lives in body.tex and
%% abstract.tex, which manuscript-ieee.tex also inputs, so the two versions
%% cannot drift apart. Edit the content there, not here.

\documentclass[preprint,11pt]{elsarticle}

\input{preamble-common}

\usepackage[hidelinks]{hyperref}   % load late
\usepackage{xurl}                  % after hyperref: let DOIs/URLs break anywhere

% Single column: every table gets the same float treatment, and \fittable
% shrinks only the ones that actually overflow.
\newenvironment{wtable}{\begin{table}[htbp]}{\end{table}}
\newenvironment{ntable}{\begin{table}[htbp]}{\end{table}}

% No venue claimed: this is a preprint, not a submission to a named journal.
% An empty \journal{} still makes elsarticle print "Preprint submitted to
% Elsevier", so the title-page footer is redefined to carry only the date.
\journal{}
\makeatletter
\def\ps@pprintTitle{%
  \let\@oddhead\@empty
  \let\@evenhead\@empty
  \def\@oddfoot{\footnotesize\itshape\hfill\today}%
  \let\@evenfoot\@oddfoot}
\makeatother

\begin{document}

\begin{frontmatter}

\title{Provenance, Not Behaviour: A Serialisation Artifact in Edge-IIoTset and
a Leakage-Free Benchmark for Precision-Agriculture Intrusion Detection}

\author[inst1]{Mostafa M. Galal}
\ead{mostafam.galal82@gmail.com}

\affiliation[inst1]{organization={Department of Computer and Systems
                                  Engineering, Alexandria University},
                    city={Alexandria},
                    country={Egypt}}

\begin{abstract}
\input{abstract}
\end{abstract}

\begin{keyword}
intrusion detection \sep industrial IoT \sep precision agriculture \sep
data leakage \sep benchmark integrity \sep federated learning \sep
edge computing \sep Edge-IIoTset
\end{keyword}

\end{frontmatter}

\input{body}

\bibliographystyle{elsarticle-num}
\bibliography{refs}

\end{document}
